// @vitest-environment jsdom
import { afterEach,describe,expect,it,vi } from 'vitest';import { cleanup,render,screen } from '@testing-library/react';import { PricingSection } from './PricingSection'
afterEach(()=>{cleanup();vi.restoreAllMocks()})
describe('PricingSection',()=>{it('renders server catalog prices and credits without product ids',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue({ok:true,json:async()=>({plans:[{slug:'starter',name:'Starter',price:14900,currency:'USD',interval:'month',includedCredits:1000,features:['Reports']},{slug:'scale',name:'Scale',price:49900,currency:'USD',interval:'month',includedCredits:5000,features:['Scale']}]})}));render(<PricingSection/>);expect(await screen.findByText((_,e)=>e?.textContent==='$149/month')).toBeTruthy();expect(screen.getByText((_,e)=>e?.textContent==='$499/month')).toBeTruthy();expect(screen.getByText((_,e)=>e?.textContent==='1,000 credits included')).toBeTruthy();expect(fetch).toHaveBeenCalledWith('http://localhost:3001/v1/billing/plans')})})
